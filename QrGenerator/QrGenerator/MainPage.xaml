<Page
    x:Class="QrGenerator.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QrGenerator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Loaded="Page_Loaded">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <WebView x:Name="webview" Grid.Column="0" Grid.Row="0"/>
        <TextBlock x:Name="txbLong" Grid.Column="0" Grid.Row="1"/>
        <TextBlock Text="Generar Qr" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" FontSize="100" TextAlignment="Center"/>
        <Button HorizontalAlignment="Center" Content="Generar" x:Name="Generar" Click="Generar_Click" Background="Aqua" Grid.Column="1" Grid.Row="1"/>
    </Grid>
</Page>
